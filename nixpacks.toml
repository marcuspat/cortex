[phases.setup]
nixPkgs = ["nodejs-20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npx prisma generate", "npm run build"]

[phases.start]
cmd = "npx prisma generate && node .next/standalone/server.js"
